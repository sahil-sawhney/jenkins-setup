infrastructure:
  override:

checkout
  override:


machine
  override:

dependencies
  override:

database
  override:

teardown
  override:

compile:
  pre:
    - sbt clean
  override:
    - sbt compile
  post:
    - sbt test:compile


test:
  override:
    - sbt test
  post:
    - sbt it:compile
 
 
